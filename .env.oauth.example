# OAuth Configuration (Recommended)
# Get these from your Outreach OAuth App settings
OUTREACH_CLIENT_ID=your_oauth_client_id_here
OUTREACH_CLIENT_SECRET=your_oauth_client_secret_here
OUTREACH_REDIRECT_URI=http://localhost:3000/callback

# Optional: API URL (defaults to https://api.outreach.io/api/v2)
# OUTREACH_API_URL=https://api.outreach.io/api/v2

# Legacy: Direct API Token (for backwards compatibility)
# If OAuth credentials are not provided, falls back to this
# OUTREACH_API_TOKEN=your_personal_access_token_here